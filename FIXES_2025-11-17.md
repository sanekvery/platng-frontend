# 🔧 Quick Fixes Reference (2025-11-17)

> Краткая справка по исправлениям. Полная документация в `CODE_REVIEW_2025-11-17.md`

---

## 🚨 Критические исправления

### 1. Production Build Failure ✅

**Ошибка:**
```
Error: not-found.tsx doesn't have a root layout
```

**Fix:**
```bash
# Создан файл: app/layout.tsx
```

**Что делать:**
- ВСЕГДА иметь `app/layout.tsx` в Next.js 14 App Router
- Проверять `npm run build` перед commit

---

### 2. ESLint Configuration ✅

**Ошибка:**
```
Invalid Options: useEslintrc, extensions...
```

**Fix:**
```json
// package.json
{
  "devDependencies": {
    "eslint": "^8.57.0",           // было: 9.39.1
    "eslint-config-next": "^14.2.33" // было: 16.0.3
  }
}
```

**Команда:**
```bash
rm -rf node_modules package-lock.json
npm install
```

---

## 🟡 ESLint Errors (9 исправлений)

| Файл | Проблема | Решение |
|------|----------|---------|
| `app/not-found.tsx` | Unescaped `'` | `&apos;` |
| `app/[locale]/layout.tsx` | `any` type | `Locale` type |
| `app/[locale]/discover/page.tsx` | Unused var | Removed `setFilters` |
| `lib/utils/formatters.ts` | Unused import | Removed `format` |
| `lib/utils/debounce.ts` | `any` in generics | `eslint-disable` |

---

## ✅ Verification

```bash
# Все команды должны проходить:
npm run type-check  # ✅ 0 errors
npm run lint        # ✅ 0 errors, 2 warnings
npm run build       # ✅ Success
```

---

## 📁 Новые/измененные файлы

### Созданные:
- `app/layout.tsx` - Root layout

### Измененные:
- `package.json` - ESLint versions
- `app/not-found.tsx` - Simplified
- `app/[locale]/layout.tsx` - Type fix
- `app/[locale]/discover/page.tsx` - Unused var
- `lib/utils/formatters.ts` - Import fix
- `lib/utils/debounce.ts` - Generics fix

---

## 🚦 Quick Checklist (перед каждым commit)

```bash
# 1. Type check
npm run type-check

# 2. Lint
npm run lint

# 3. Build production
npm run build

# Если все ✅ - можно коммитить
```

---

## 🔗 Полная документация

- 📊 **CODE_REVIEW_2025-11-17.md** - Детальный отчет ревью (метрики, анализ, рекомендации)
- 🔍 **INSIGHTS_AND_PREVENTION.md** - Почему возникли проблемы и как предотвратить
- 🚀 **START_HERE.md** - Quick start guide (обновлен)

---

**Дата:** 2025-11-17
**Статус:** ✅ Все исправлено и протестировано
